<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Add Blog</title>
  <link rel="stylesheet" href="output.css">
</head>
<body>

<!-- navbar -->
<%- include('partials/nav') %>

<div class="max-w-md mx-auto bg-white p-6 rounded-xl shadow-md my-10">
  <h2 class="text-2xl font-bold text-center mb-6">Add New Blog</h2>

  <form action="/register" method="POST" class="space-y-4">

    <!-- Blog Heading -->
    <div>
      <label class="block text-gray-700 font-semibold mb-1">Blog Heading</label>
      <input
        type="text"
        name="heading"
        placeholder="Enter blog heading"
        class="w-full border border-gray-300 rounded-lg p-2"
        required
      >
    </div>

    <!-- Tech (Text Input) -->
    <div>
      <label class="block text-gray-700 font-semibold mb-1">Tech</label>
      <input
        type="text"
        name="tech"
        placeholder="e.g. Programming, Web, AI"
        class="w-full border border-gray-300 rounded-lg p-2"
        required
      >
    </div>

    <!-- Blog Text / Description -->
    <div>
      <label class="block text-gray-700 font-semibold mb-1">Blog Text</label>
      <input
        type="text"
        name="text"
        placeholder="blog text"
        class="w-full border border-gray-300 rounded-lg p-2"
        required
      >
    </div>

    <!-- image url -->
    <div>
      <label class="block text-gray-700 font-semibold mb-1">Blog image</label>
      <input
        type="text"
        name="photo"
        placeholder="image url"
        class="w-full border border-gray-300 rounded-lg p-2"
        required
      >
    </div>

    <!-- Submit -->
    <button
      type="submit"
      class="w-full bg-indigo-500 text-white font-semibold py-2 rounded-lg hover:bg-indigo-600 transition"
    >
      Publish Blog
    </button>

  </form>
</div>

</body>
</html>
